import React from "react";
import ReactDOM from "react-dom/client";

// import App from "./App.jsx";

//import current component here, once finished, comment below code and uncomment upper App import
import App from "./components/CollapsingText.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
